# Design Review — 2026-09-26 (Phase 0 → UC-003, Phase 2 in progress)

> **Reviewer:** assistant-led review; user evaluation confirmed all findings valid (2026-09-26).
> **Scope:** cross-document consistency of Phase 0 (`subcomponents/README.md`), Phase 1a (`contracts/task-job-management.md`), Phase 1b (`contracts/frontend-odb-api.md`), Phase 2 (`usecases/UC-001…003`), checked against the decision log (`../README.md` §7) — in particular the late decisions **D-014, D-019, D-020** and their propagation.
> **Method:** `Design Review Procedure.md` (this folder) — decision-propagation check + Traceability Check Strategy (5 checks) + mechanical grep (ID counts, superseded D-* references, stale "provisional" markers, orphan IDs).
> **Excluded (deferred-by-design, not findings):** old UC drafts (P2-001 — archive at Phase-2 close) · UC-004…011 (in-flight) · starvation/unbounded thresholds (D-007/D-010) · D-017 payload schema table (deferred with Phase 2).

## Findings

| ID | Sev | Finding | Location | Recommendation | Status | Resolution |
| --- | --- | ------- | -------- | -------------- | ------ | ---------- |
| RV-01 | high | `SCR-C1-001-002` body still states **C1 reads/parses `gdoc.project.json`** and hands the structure to the ODB — contradicts **D-019** (ODB owns config) and this same file's Main #5/#9/#10, Reverse-check, and Traceability (C1 forwards workspace root only) | `usecases/UC-001_OpenWorkspace.md` §SCR-C1-001-002 | Reword the SCR: C1 forwards workspace root + config location; **shall not** read/parse the config (D-019) | [F] | Applied: SCR body is now forward-only (C1 forwards workspace root + config location in API-object creation and `CONFIG_SAVE`; **shall not** read/parse; D-019). Verified: `grep -c 'C1 shall read' UC-001` → 0. |
| RV-02 | high | `SCR-C2-001-005` is present in the Reverse-check table and the Traceability Matrix but its **body definition block is missing** (orphan IDs) | `usecases/UC-001_OpenWorkspace.md` between §SCR-C2-001-004 and §SCR-C3-001-001 | Add the missing `SCR-C2-001-005` block: C2 reads/parses config; defines Project scope / Packages / content types (D-019, ADR-009, INV-06) | [F] | Applied: block added (C2 reads/parses config; Project scope / Packages / content types; degraded mode per EH-001-001). Verified: `grep -c 'SCR-C2-001-005'` → 4 (definition + Reverse-check + Traceability + cross-reference). |
| RV-03 | high | `EH-001-001` body assigns handling to **C1** (Owner: C1), while Traceability / Gap analysis / Alt scenario say **C2 detects** (D-019) and C1 notifies | `usecases/UC-001_OpenWorkspace.md` §EH-001-001 | Split: C2 detects absence + degraded mode; C1 relays to IDE Client; Owner: C2 · C1 | [F] | Applied: body now C2 detects + degraded mode, C1 relays to IDE Client; `**Owner:** C2 (detection + degraded mode) · C1 (notification)`. |
| RV-04 | low | Preconditions assert `gdoc.project.json` **exists**, while Alt "No Project Configuration Found" analyzes its absence | `usecases/UC-001_OpenWorkspace.md` §Preconditions | Make config presence a handled condition, not a precondition | [F] | Applied: precondition now "may or may not exist — its absence is a handled condition (Alt …, EH-001-001)". |
| RV-05 | med | §6 traceability tables lack **TJ-021** in *both* directions (`TJ-021 → …` row and `D-014 → TJ-021` row); §7 checklist still says "20 rules, TJ-001…TJ-020" | `contracts/task-job-management.md` §6, §7 | Add both rows (TJ-021: FR-3.1/3.2 → ADR-002/007 → D-014 · R-006-3/R-008-1); §7: "21 rules, TJ-001…TJ-021" | [F] | Applied: §6 rows `TJ-021 \| FR-3.1/3.2 \| ADR-002, 007 \| R-006-3, R-008-1 \| D-014` and `D-014 → TJ-021` added; §7 now "21 rules, TJ-001…TJ-021". Verified: `grep -c '^| TJ-0'` → 42 (= 21 rules in §4 + 21 in §6); `grep -c '20 rules'` → 0. |
| RV-06 | low | §9 table cites `TJ-018 (D-004)` although **D-020 supersedes** D-004's tuple; the D-011 "*provisional — to be logged at approval*" marker remains in §10.1 M2 **and** in API-003's Derived-From, although D-011 is logged (`../README.md` §7, 2026-09-13; cf. document header L5) | `contracts/frontend-odb-api.md` §9, §10.1 (M2), API-003 | §9: `TJ-018 (D-020, refines D-004)`; M2 + API-003: "logged in `../README.md` §7 (2026-09-13)" | [F] | Applied: all three spots updated. Verified: `grep -c 'TJ-018 (D-004)'` → 0; `grep -c 'provisional — to be logged'` → 0. |
| RV-07 | low | §7 self-check S1/S5 (and §1 ID note) still count "INV-01…INV-29" although §4.1 defines **INV-30** (D-020) | `subcomponents/README.md` §7 (S1, S5), §1 | Update to "INV-01…INV-30" | [F] | Applied: §1 ID note + S1 + S5 now "INV-01…INV-30". Verified: `grep -c 'INV-01…INV-29'` → 0; `grep -c 'INV-01…INV-30'` → 3. |

## Verdict

The review found **no design errors**: all 7 items are propagation/notation drift from later decisions (D-014, D-019, D-020) that did not reach a body text or a table — exactly the class this derivation discipline must catch **before Phase 3 derives `LSP-*`/`ODB-*` from the `SCR-*` rows** (RV-01…03 in particular). RV-04…07 are mechanical notation/traceability repairs.

**No new user decisions required** — every fix applies already-confirmed decisions (D-014 / D-019 / D-020); nothing is deferred.

## Positive observations (no action)

- State-relative `version_id` (D-020) is consistently applied in UC-002/UC-003 (C1 forwards the raw `open_revision` fact; C2 owns selection/bookkeeping — INV-05/INV-30, TJ-018).
- System Task lifecycle (D-014 / TJ-021) flows without contradiction across UC-001 (State 3 creation), UC-002 (State 2 creation), UC-003 (kept unchanged on edit).
- The reverse-check mechanism works as intended (NC-06 → P2-003; UC-003 → D-021 note on TJ-012).

## Fix log

> **Applied 2026-09-26** (all 7 findings [F]): commit `docs: apply design review 2026-09-26 findings (RV-01…RV-07)` — trace with `git log --grep 'RV-0'`. Per-finding verification evidence is recorded in the Resolution column above (mechanical checks per `Design Review Procedure.md` §2.3, this folder).
